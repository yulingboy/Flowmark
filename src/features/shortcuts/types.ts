/**
 * 快捷方式模块类型定义
 */

/** 网格配置 */
export interface GridConfig {
  columns: number;
  rows: number;
  unit: number;
  gap: number;
}

// ValidationResult 和 SHORTCUTS_LIMIT 定义在 store/types.ts 中
